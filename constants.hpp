#ifndef CONSTANTS_HPP
#define CONDTANTS_HPP

#include <string>

namespace constants {
    std::string DEFAULT_PATH_TO_OUTPUT = "output.txt";

    std::string DEFAULT_PATH_TO_INPUT = "input.txt";
}
#endif